openai==1.3.8
python-dotenv==1.0.0
pyaudio==0.2.13
numpy==1.24.3
elevenlabs==0.2.26
pydub>=0.25.1
simpleaudio>=1.0.4
tiktoken==0.5.2
requests==2.31.0
beautifulsoup4==4.12.2
lxml==4.9.3

# Core web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0

# Firebase
firebase-admin==6.2.0
google-cloud-firestore==2.13.1

# Audio processing
wave

# Web scraping
# (requests, beautifulsoup4, lxml are already listed above)

# Text processing
# (tiktoken is already listed above)

# Environment management
# (python-dotenv is already listed above)

# CORS middleware
python-multipart==0.0.6

# Temporary file handling (built-in, but listing for clarity)
tempfile

# Threading and time (built-in)
threading
time
os
sys
json

# Additional utilities
python-dateutil==2.8.2
